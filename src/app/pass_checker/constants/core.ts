export enum PasswordErrors {
  SHORT = "Password is too short",
  NO_UPPERCASE = "Password has no uppercase letter",
  NO_LOWERCASE = "Password has no lowercase letter",
  NO_NUMBER = "Admin password has no number",
}

export const NUMBER_REGEX = /\d/;
